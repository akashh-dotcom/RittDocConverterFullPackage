@using System.Web.Optimization
@using R2V2.Web.Helpers
@model R2V2.Web.Models.IR2V2Model

@{
    var header = Model.Header;
}
<!doctype html>
<!--[if IE 7]>    <html class="no-js ie7" lang="en"> <![endif]-->
<!--[if IE 8]>    <html class="no-js ie8" lang="en"> <![endif]-->
<!--[if IE 9]>    <html class="no-js ie9" lang="en"> <![endif]-->
<!--[if gt IE 9]><!-->
<html class="no-js" lang="en">
 <!--<![endif]-->
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />

    <title>@ViewBag.Title | R2 Digital Library</title>

    <meta name="description" content="" />
    <meta name="keywords" content="" />

    @if (IsSectionDefined("CssIncludes"))
    {
        @RenderSection("CssIncludes")
    }
    <link rel="stylesheet" href="@BundleTable.Bundles.ResolveBundleUrl("~/_Static/bundles/css")" />
    @if (IsSectionDefined("PrintCssIncludes"))
    {
        @RenderSection("PrintCssIncludes")
    }


    <script src="@Url.Script("libraries/modernizr.custom.js")"></script>
</head>
<body style="width: 100%" class="@Html.Raw(ViewBag.BodyCssClass == "" ? "" : ViewBag.BodyCssClass) @Model.Layout.ControllerName @Model.Layout.ActionName">


<header role="banner" style="width: 100%">
    <div class="clearfix masthead" style="width: 100%">
        <div class="branding-main">
            <a href="/"><img alt="R2 Digital Library" src="@Url.Image("r2-digital-library.png")" height="67"/></a>
        </div>

        @if (!string.IsNullOrWhiteSpace(Model.EnvironmentName))
        {
            <div class="env">@Model.EnvironmentName</div>
        }

        <div class="utilities full">
            @if (Model.AuthenticationInfo.IsAuthenticated)
            {
                @Html.Partial("_AuthInfo", Model.AuthenticationInfo)
            }

            @if (header.ShowLoginLink)
            {
                <div id="auth-confirmation"></div>

                @Html.Partial("_LoginForm", header.LoginParam)
            }
        </div>
    </div>
</header>



<div id="container" class="clearfix">
    @Html.Partial("_TopNav")

    @RenderBody()

    @Html.Partial("_Footer")
</div>

@if (IsSectionDefined("InlineModal"))
{
    @RenderSection("InlineModal")
}


    @* JS namespacing *@<script>
                            var R2V2 = R2V2 || {};
                            R2V2.Config = {
                                IsMarketingHome: "false",
                                Tracking: { Category: '', Action: '' },
                                DisableRightClick: "false"
                            };
                        </script>

@* Grab Google CDN's jQuery, with a protocol relative URL; fall back to local if offline *@
<script src="//ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js"></script>
<script>window.jQuery ||
    document.write('<script src="@Url.Script("libraries/jquery-1.7.1.min.js")"><\/script>')
</script>

<script src="@BundleTable.Bundles.ResolveBundleUrl("~/_Static/bundles/js")"></script>


@if (IsSectionDefined("JsIncludes"))
{
    @RenderSection("JsIncludes")
}

<script>
    ;
    (function($) {
        $(function() {
            @if (IsSectionDefined("JsDocumentReady"))
            {
                @RenderSection("JsDocumentReady")
            }
        });
    })(jQuery);
</script>

<script>
    var _gaq = [['_setAccount', '@Model.Layout.GoogleAnalyticsAccount'], ['_trackPageview']];
    (function(d, t) {
        var g = d.createElement(t), s = d.getElementsByTagName(t)[0];
        g.src = ('https:' == location.protocol ? '//ssl' : '//www') + '.google-analytics.com/ga.js';
        s.parentNode.insertBefore(g, s);
    }(document, 'script'));
</script>
</body>
</html>